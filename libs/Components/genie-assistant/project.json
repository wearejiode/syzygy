{
  "name": "genie-assistant",
  "projectType": "application",
  "sourceRoot": "apps/Core/Api/genie-assistant",
  "targets": {
    "build": {
      "executor": "@nx/node:build",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/apps/Core/Api/genie-assistant",
        "main": "apps/Core/Api/genie-assistant/src/main.ts",
        "tsConfig": "apps/Core/Api/genie-assistant/tsconfig.app.json"
      }
    },
    "serve": {
      "executor": "@nx/node:execute",
      "options": {
        "buildTarget": "components:genie-assistant:build"
      }
    }
  },
  "tags": ["type:ui", "scope:components"]
}
