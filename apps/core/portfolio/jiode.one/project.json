{
  "name": "@jiode/jiode.one",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          {
            "command": "vite build",
            "cwd": "apps/core/portfolio/jiode.one"
          }
        ]
      }
    }
  }
}
