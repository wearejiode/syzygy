{
  "name": "auth",
  "projectType": "application",
  "sourceRoot": "apps/Core/Api/auth",
  "targets": {
    "build": {
      "executor": "@nx/node:build",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/apps/Core/Api/auth",
        "main": "apps/Core/Api/auth/src/main.ts",
        "tsConfig": "apps/Core/Api/auth/tsconfig.app.json"
      }
    },
    "serve": {
      "executor": "@nx/node:execute",
      "options": {
        "buildTarget": "api:auth:build"
      }
    }
  },
  "tags": ["scope:core", "type:api"]
}
